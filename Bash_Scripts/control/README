host配置文件
	配置文件格式： 别名 地址 SSH端口 用户名
	#别名前有一个空格，每个参数都用空格隔开
	#别名：可以自定义，建议最好命名与主机相关的别名
	#别名保留关键词：add
	#请注意：如果别名设置为保留关键字，那么关键词相应功能将无法使用
	#地址：可以是ip、域名等，需要本机可以解析到的地址
	#SSH端口：必须输入SSH端口，一般是22
	#用户名：必须输入登录主机的用户名

key文件夹
	#没有key文件夹时需要手动创建
	#key文件夹下的每个密钥需要与对应主机的别名相同
